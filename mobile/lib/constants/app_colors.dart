import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - من اللوغو والتصميم
  static const Color primary = Color(0xFF2FA884);
  static const Color primaryDark = Color(0xFF2A504C);

  // Background
  static const Color background = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF5ED388);

  // Button Colors
  static const Color buttonBorder = Color(0xFF2A504C);
  static const Color buttonHover = Color(0xFF2FA884);
}